{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "DATABASE_URL=${DIRECT_URL:-$DATABASE_URL} npx prisma db push --skip-generate --accept-data-loss || echo 'DB push skipped' && node dist/src/main.js",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
